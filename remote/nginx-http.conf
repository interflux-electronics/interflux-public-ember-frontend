server
{
  listen 80;
  listen [::]:80;

  server_name new.interflux.com;

  root /var/www/new.interflux.com/dist;
  index index.html;

  # For Ember apps with Fastboot
  # location / {
  #   proxy_pass http://127.0.0.1:8000;
  # }

  # For Ember apps without Fastboot
  location / {
    try_files $uri /index.html;
  }
}
