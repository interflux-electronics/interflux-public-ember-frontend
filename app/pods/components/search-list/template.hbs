<div
  class="search-list"
  {{did-insert this.onInsert}}
>
  <Input::String
    @type="text"
    @onChange={{this.filter}}
    @placeholder={{@input-placeholder}}
    @class={{@input-class}}
  />
  <p>{{this.count}} / {{this.max}}</p>
  {{yield}}
</div>