<header id='mobile' role='banner' aria-label='site' class={{this.classes}}>
  <nav aria-label='page'>
    <div id='top'>
      {{#if @backRoute}}
        <Button
          @route={{@backRoute}}
          @model={{@backModel}}
          @icon='chevron-left'
          @theme='square outlined'
        />
      {{else}}
        <div id='logo'>
          <div class='clip'>
            <Svg::PrimaryInterfluxLogo />
          </div>
        </div>
      {{/if}}
      <h1 aria-hidden='true'>{{@title}}</h1>
      <Button
        @icon='hamburger'
        @theme='square outlined {{if this.menuIsOpen 'close'}}'
        @onClick={{this.toggleMenu}}
      />
    </div>

    <div id='mid'>
      {{#if @backRoute}}
        <div id='logo'>
          <Svg::PrimaryInterfluxLogo />
        </div>
      {{/if}}

      <Button
        @route='home.products'
        @text='Products'
        @theme='stack white'
        @icon='flask'
        @onClick={{this.closeMenu}}
      />
      <Button
        @route='home.webinars'
        @text='Webinars'
        @theme='stack white'
        @icon='podcast'
        @onClick={{this.closeMenu}}
      />
      <Button
        @route='home.company'
        @text='Company'
        @theme='stack white'
        @icon='microchip'
        @onClick={{this.closeMenu}}
      />
      <Button
        @route='documents'
        @text='Documents'
        @theme='stack white'
        @icon='document'
        @onClick={{this.closeMenu}}
      />
      <Button
        @route='contact'
        @text='Contact'
        @theme='stack white'
        @icon='street-view'
        @onClick={{this.closeMenu}}
      />
    </div>
  </nav>
</header>
