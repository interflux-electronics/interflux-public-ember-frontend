server
{
  listen 80;
  listen [::]:80;

  server_name interflux.at;
  server_name interflux.com.de;
  server_name interflux.de.com;
  server_name interflux.de;
  server_name www.interflux.at;
  server_name www.interflux.com.de;
  server_name www.interflux.de.com;
  server_name www.interflux.de;

  return 301 https://interflux.de$request_uri;
}

server
{
  listen 443 ssl http2;
  listen [::]:443 ssl http2;

  server_name www.interflux.de;

  ssl_certificate /etc/letsencrypt/live/www.interflux.de/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/www.interflux.de/privkey.pem;
  include /etc/letsencrypt/options-ssl-nginx.conf;
  ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

  return 301 https://interflux.de$request_uri;
}

server
{
  listen 443 ssl http2;
  listen [::]:443 ssl http2;

  server_name interflux.de;

  root /var/www/interflux.de/dist;
  index index.html;

  # 1. First, check the exact path (robot.txt, favicon.ico, ...)
  # 2. Second, attempt server side render + hydration (Ember Fastboot)
  # 3. Third, attempt client side render (Ember)
  
  location / {
    try_files $uri @fastboot; # Do not add /index.html at the end.
  }

  location @fastboot {
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $remote_addr;

    # interflux.com = 4800
    # interflux.de = 4801
    # interflux.es = 4802
    # interflux.fr = 4803
    # interflux.mx = 4804
    # interflux.cn.com = 4805
    proxy_pass http://127.0.0.1:4801;

    # When Ember Fastboot (SSR) fails to respond, serve Ember classic (CSR).
    proxy_intercept_errors on;
    error_page 404 500 502 =200 /index.html;
  }

  error_page 500 /500.html;
  error_page 502 /500.html;
  error_page 404 /500.html;

  ssl_certificate /etc/letsencrypt/live/interflux.de/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/interflux.de/privkey.pem;
  include /etc/letsencrypt/options-ssl-nginx.conf;
  ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

  access_log /var/www/interflux.de/logs/nginx/access.log;
  error_log /var/www/interflux.de/logs/nginx/error.log;
}
